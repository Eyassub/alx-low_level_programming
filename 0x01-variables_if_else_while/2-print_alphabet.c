#include <stdio.h>
/**
* main - gives alphabets in lowercase from a to z
* Return: 0 is a success
*/
int main(void)
{
Char ch;
for (ch = 'a'; ch <= 'z'; ch++);
{
putchar(ch);
}
putchar('\n') 
return (0)
}
