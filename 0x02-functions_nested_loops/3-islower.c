#include "main.h"
/**
* int _islower(int c) - shows if the characters are in lowercase
* Retun: 1 if it is lowercase and 0 if not
*/
int _islower(int c);
{
if (c >= a && C <= z)
return (1)
else
return (0)
}
